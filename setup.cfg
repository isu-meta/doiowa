[metadata]
name=doiowa
version=2025.12.5

[options]
package_dir=
    =src
packages=find:
install_requires=
    crossrefapi
    lxml
    nameparser
    requests

[options.packages.find]
where=src

[options.entry_points]
console_scripts=
    doiowa=doiowa.__main__:main