[metadata]
name=doiowa
version=2026.4.8

[options]
package_dir=
    =src
packages=find:
install_requires=
    crossrefapi
    lxml
    nameparser
    requests

[options.packages.find]
where=src

[options.entry_points]
console_scripts=
    doiowa=doiowa.__main__:main